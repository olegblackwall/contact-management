import { createStore } from 'vuex'
import modal_module from './modal/modal'

const store = createStore({
    state: {},
    mutations: {},
    modules: {
        modal_module,
    },
    actions: {}
})

export default store
